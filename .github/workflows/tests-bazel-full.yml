name: Bazel full suite

on:
  schedule:
    - cron: "17 3 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  full_suite:
    name: Full Bazel suite
    uses: ./.github/workflows/tests-bazel.yml
    with:
      run_full: true
      run_impacted: false
    secrets: inherit
