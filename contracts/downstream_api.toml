# Symbols that downstream repos import from gtm-sdk.
#
# A symbol listed here is public API: it may be entirely unreferenced inside
# this repo and still be load-bearing. Do not delete one because a dead-code
# scan called it unused — see AGENTS.md, "Public API and downstream consumers".
#
# Generated file. Regenerate rather than hand-edit:
#   scripts/downstream-contract-sync.py <consumer-checkout> --write
#
# Enforced by tests/test_downstream_contract.py, which runs in the required
# `Unit tests` gate.

[consumers."crm-uploader"]
description = "downstream consumer: crm-uploader"

[consumers."crm-uploader".modules]
"libs.attio" = [
  "AttributeCreateResult",
  "CompanyInput",
  "EXT_TAM_OBJECT",
  "ExtTamInput",
  "GTM_CONTENT_OBJECT",
  "GtmContentInput",
  "NoteInput",
  "PersonInput",
  "ReliabilityEnvelope",
  "build_patch_record_request",
  "create_attribute",
  "create_note",
  "create_object",
  "describe_attio_error",
  "ensure_select_options",
  "find_by_person_and_account",
  "find_company_by_domain",
  "find_company_by_name",
  "find_person_by_name_at_company",
  "find_person_by_sanity_author_id",
  "format_company_description",
  "format_company_domains",
  "format_company_linkedin",
  "format_location_from_parts",
  "get_client",
  "is_select_attribute_writable",
  "list_select_options",
  "search_people",
  "set_company_owner",
  "set_person_sanity_author_id",
  "stub_create_company",
  "stub_create_person",
  "upsert_ext_tam",
  "upsert_gtm_content",
  "upsert_person",
]
"libs.attio.people" = ["_search_people_raw"]
