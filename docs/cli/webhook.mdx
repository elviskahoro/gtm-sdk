---
title: "gtm webhook"
description: "Command reference for gtm webhook — Webhook URL registry (Modal endpoints + Hookdeck wiring). Generated from the CLI help text."
---

{/* AUTOGENERATED by scripts/docs-cli_reference-generate.py — do not edit. Edit the help= strings in cli/ and rerun: uv run scripts/docs-cli_reference-generate.py */}

Webhook URL registry (Modal endpoints + Hookdeck wiring).

Use the command sections below as the source of truth for supported options and output behavior.

## Commands

### `gtm webhook sync`

Regenerate webhooks/registry.yaml from live Modal + Hookdeck state.

```bash
uv run gtm webhook sync [OPTIONS]
```

### `gtm webhook list`

Print the cached registry as JSON. Errors if no cache; run `sync` first.

```bash
uv run gtm webhook list [OPTIONS]
```
